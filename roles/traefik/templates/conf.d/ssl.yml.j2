# {{ansible_managed}}

{% if not traefik_acme %}
tls:
  stores:
    default:
      defaultCertificate:
        certFile: {{openstack_traefik.cert_path}}
        keyFile: {{openstack_traefik.privatekey_path}}
{% else %}
# automated by let's encrypt
{% endif %}
