127.0.0.1   localhost localhost.localdomain localhost4 localhost4.localdomain4
::1         localhost localhost.localdomain localhost6 localhost6.localdomain6

# {{ansible_managed}}

# openstack nodes
{% for node in groups.openstack %}
{{hostvars[node].management_ipv4.address}} {{hostvars[node].ansible_nodename}}
{% endfor %}
{% if bootstrap_extra_hosts | length > 0 %}

# extra hosts
{% for host in bootstrap_extra_hosts %}
{{host.ip_address}} {{host.name}}
{% endfor %}
{% endif %}
